import React from 'react';
import styles from './VoteResult.module.css';

const VoteResult = (props) => {
  return  <section className={styles.container}>1초동안 로딩페이지 띄우고 그 다음 투표결과 랜딩</section>
}
export default VoteResult;